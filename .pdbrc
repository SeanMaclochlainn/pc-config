import os
exec(open(os.path.expanduser("~/.pdbrc_utils.py"), 'rU').read())

# Print a dictionary, sorted. %1 is the dict, %2 is the prefix for the names.
alias p_ for k in sorted(%1.keys()): print(f"%2{k.ljust(max(len(s) for s in %1.keys()))} = {%1[k]}")

# Print the member variables of a thing.
alias pi p_ %1.__dict__ %1.

# Print the member variables of self.
alias ps pi self

# Print the locals.
alias pl p_ locals() local:

# Next and list, and step and list.
alias nll n;;ll
alias nl n;;l
alias sl s;;l

# alias v pp vars(%1)

alias d pp dir(%1)

# alias v pp %1 if type(%1)==dict or not hasattr(%1, '__dict__') else vars(%1)


# pr = print('\033[95m');pprint.pp(%1);print('\033[0m')
# alias v pprint.pp(%1) if type(%1)==dict or not hasattr(%1, '__dict__') else pprint.pp(vars(%1))


alias v view_item(%1)
alias pt view_table(%1)
alias i import os;exec(open(os.path.expanduser("~/.pdbrc_utils.py"), 'rU').read())
